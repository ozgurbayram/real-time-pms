export enum TaskStaus {
  DRAFT = 'draft',
  ASSIGNED = 'assigned',
  IN_PROGRESS = 'in_progress',
  WAITING_APPROVAL = 'waiting_approval',
  IN_TEST = 'in_test',
  DONE = 'done',
  CLOSED = 'closed',
}
